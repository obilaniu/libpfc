# The Performance Counter Library
project('libpfc', ['c'],
        version:                 '0.0.0',
        meson_version:           '>=0.41.0',
        default_options:         [])

subdir('include')
subdir('src')
subdir('kmod')
